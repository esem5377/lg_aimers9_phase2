# 2026-08-22

## 배경
- 세션 시작 시점 팀 최고는 974점(es_ws v7, 8/19 종료 상태)이었으나, `git pull`로
  jh_ws 쪽 8/21~22 세션 커밋(`v18_seed_bagging`)을 받아보니 이미 **982점**으로
  갱신돼 있었음(974 레시피를 6개 랜덤시드로 배깅 -- 새 피처 없이 순수 분산
  감소, 로컬 fold0/fold2 델타와 실제 리더보드 델타가 거의 정확히 일치한
  드문 사례). jh_ws 결론: "새 피처 추가류는 사실상 다 막혔다", 남은 저기대
  후보는 시드 추가/OOF 스태킹 정도.

## 한 일

### 1. 아키텍처 블렌드(CatBoost+LGBM+XGBoost) walk-forward 재검증
- jh_ws `v16_ensemble/diag_ensemble.py`가 season<=2023/==2024 단일 분할에서
  블렌드 +2.90을 보고했었지만, 이 프로젝트 기준(fold0/fold2 두 독립 폴드
  일치해야 신뢰 -- 단일 폴드 신호는 regime2023/freq974 등에서 반복 반증됨)을
  통과 못 한 상태였음.
- `work/experiments/tune_arch_blend_walkforward.py` 신규 작성 — fold0(→2022)
  /fold2(→2024)로 확장 재검증. 결과: fold0 delta=+5.42, fold2 delta=+2.08,
  **둘 다 양수**. 다만 블렌드 가중치를 eval셋에 직접 fit해 낙관편향 있는
  진단(jh 원본과 동일 관례).
- `work/experiments/tune_arch_blend_honest.py`로 낙관편향 제거 재검증 —
  각 fold의 valid_season을 calib_fit/calib_eval로 50/50 분리해 calib_fit에서만
  가중치+Platt을 고르고 calib_eval에서 평가. **fold0 +4.57, fold2 +1.37,
  여전히 둘 다 양수**로 확인. 가중치는 fold별로 cat 0.7~0.85 / lgb 0.1~0.15
  / xgb 0.05~0.15 범위.

### 2. 프로덕션 후보 빌드 — 첫 시도에서 함정 발견 및 수정
- `work/pipeline/train_arch_blend_bagged.py` 작성 — jh_ws의 기존 CatBoost
  6시드 모델(982점 자산, 재학습 없이 그대로 로드)에 신규 학습한 LGBM
  3시드 + XGBoost 3시드를 더해 블렌드하는 프로덕션 스크립트.
- **1차 시도(버그)**: 5% 무작위 calibration carve-out에서 블렌드 가중치를
  직접 grid search했더니 XGBoost가 CatBoost를 역전(raw BSS 2115 vs 2057)하고
  가중치가 `xgb 0.85`로 쏠려 블렌드 효과가 사실상 0(+0.12)으로 나옴. 원인:
  이 carve-out은 전체 2019~2024에서 무작위로 뗀 것이라 train과 선수풀/분포를
  공유 — jh_ws가 세션 초반(8/16)에 겪은 "무작위분할 OOF가 미래시즌
  일반화를 과대평가"하는 함정이 그대로 재현된 것. 이 결과는 폐기.
- **수정**: 블렌드 가중치는 carve-out에서 재탐색하지 않고 §1의 walk-forward
  honest 결과(fold2 가중치: `cat 0.85 / lgb 0.1 / xgb 0.05`, 최근/성숙
  데이터 레짐이라 채택)를 그대로 고정 사용. carve-out은 원래 취지대로
  Platt 보정에만 사용.
- 같은 calib carve-out에서 CatBoost 단독과 apples-to-apples 재비교:
  calibrated BSS **2069.21 → 2080.01 (+10.80)**.

### 3. 제출 — 982 → 986 (+4)
- `submissions/submit_v9_arch_blend/` 패키징(CatBoost 6시드 + LGBM 3시드 +
  XGBoost 3시드 + feature_meta.json, script.py는 아키텍처별 배깅 후 고정
  가중치 블렌드 → Platt 보정 1회). 클린룸 검증(zip 재추출 후 재실행, test.csv
  5건 예측값 완전 동일, OOV pitcher_id 3건/batter_id 2건으로 v7/v18과 일치)
  통과.
- **1차 제출 실패**: `xgboost==3.4.1`(pip 최신 릴리스) 버전 고정이 평가
  서버 미러에 없어 `InstallError: No matching distribution found`. `catboost==1.2.10`
  /`lightgbm==4.7.0`은 과거 실제 제출(822/974/982점)에서 이미 검증된 조합이라
  문제 없었고, xgboost만 이 프로젝트에서 처음 시도하는 신규 의존성이었음.
  버전 고정 제거(`xgboost`만 남김)로 수정 후 재제출.
- **리더보드 결과: 986점** (982 대비 +4, §1 walk-forward honest 델타(+1.37~
  +4.57)와 거의 같은 크기 — 새 피처가 아니라 시드 배깅과 같은 계열(모델
  분산 감소)이라 로컬-리더보드 괴리가 작았던 것으로 해석. 다만 이 프로젝트엔
  fold0/fold2 정직 이중 일치도 리더보드에서 반증된 전례(freq974)가 있어
  매번 실측 필수라는 원칙은 유지.

## 4. 진짜 OOF 스태킹 검증 — 기각
- §1의 후속으로 "그리드서치 블렌드 대신 LogisticRegression 메타러너로
  스태킹하면 더 나을까"를 `work/experiments/tune_arch_stacking_honest.py`로
  검증(동일 fold0/fold2 + calib_fit/calib_eval 하네스, calib_fit에서
  raw 예측 3개를 피처로 LR fit).
- **결과: fold0 -13.85, fold2 -29.21 — 그리드서치 블렌드보다 뚜렷하게
  나쁨. 심지어 단일 최고 모델보다도 못함(-9.28/-27.83).** 3개 모델 raw
  예측이 서로 강하게 상관돼 있어 제약 없는 선형결합이 calib_fit
  소규모 데이터에 과적합한 것으로 추정. jh_ws가 "모델간 상관 높아 기대
  낮다"고 판단만 하고 미검증이었던 가설이 실측으로 확인됨. **기각,
  프로덕션 미반영.**

## 5. 시드 배깅 확장(9/6/6) 시도 — 중단(더 나은 베이스 발견으로 무의미해짐)
- `work/pipeline/train_arch_blend_bagged_v2.py`로 CatBoost 6→9시드,
  LGBM/XGBoost 3→6시드 확장을 시작했으나, 이 머신에서 예상(약 40분)과
  달리 3시간 40분+ 실행 중(스레드는 살아있고 CPU도 계속 소모 중이라
  멈춘 건 아니었음 — 원인 미상, 8/19에 기록된 "CPU CatBoost 학습 먹통"류
  불안정성과는 다른 양상). §6에서 jh_ws가 더 나은 베이스 레시피(992점)를
  push해 이 작업 자체의 가치가 없어져 강제 종료, 재조사 안 함.

## 6. jh_ws 992점 갱신 반영 — control_risk_score 피처
- 세션 중 `git pull`로 jh_ws `v20_control_risk_score` 커밋 확인:
  982 레시피에 `asof_pitcher_reverse_rate/middle_rate/ball_rate`를 재조합한
  `control_risk_score`(단순합) / `control_risk_score_weighted`(0.4/0.3/0.3
  가중합) 2개 파생 피처 추가. 로컬 검증(iterations=1000)은 fold0 -3.74/
  fold2 +7.04로 방향이 엇갈렸는데도(=이 프로젝트 채택 기준 미충족) 사용자
  판단으로 제출 진행 → **실제 리더보드 982 → 992 (+10)**. 방향이 엇갈린
  로컬 신호가 실측에서는 양수였던 드문 사례.

## 7. 992 레시피 위에 아키텍처 블렌드 재적용 — 986 → 새 최고
- §1~3의 CatBoost+LGBM+XGBoost 블렌드 기법을 992 레시피(§6) 위에서
  `work/experiments/tune_arch_blend_honest_riskscore.py`로 fold0/fold2
  walk-forward + calib_fit/calib_eval 정직 분리 재검증.
- **결과: fold0 +3.02, fold2 +3.00 — 이 프로젝트에서 보기 드물게 두 폴드가
  거의 정확히 같은 크기로 일치.** 가중치는 cat 0.75~0.8 / lgb 0.2 /
  **xgb는 두 폴드 다 0에 가까움(0.05, -0.0)** — v9(986점) 때와 달리 이번
  피처셋에서는 XGBoost 기여가 사실상 없음.
- `work/pipeline/train_riskscore_blend.py`로 프로덕션 빌드: jh_ws v20의
  CatBoost 6시드(992점 자산, 재학습 없음)에 신규 LGBM 3시드만 추가, XGBoost는
  제외(가중치 0 근처라 드롭, xgboost 버전 설치 이슈 전례도 있어 의존성도
  줄임). 가중치는 walk-forward 평균(cat 0.78/lgb 0.22) 고정 사용(carve-out
  재탐색 안 함, §2의 낙관편향 교훈 유지).
- 같은 calib carve-out에서 CatBoost 단독과 apples-to-apples 비교:
  calibrated BSS **2068.46 → 2070.44 (+1.98)**.
- `submissions/submit_v11_riskscore_blend/` 패키징, 클린룸 검증(zip
  재추출 후 재실행, test.csv 5건 예측값 완전 동일, OOV pitcher_id 3건/
  batter_id 2건으로 기존과 일치) 통과.
- **리더보드 제출 결과: 993점** (992 대비 +1). 로컬 apples-to-apples
  예측(+1.98)과 방향·자릿수가 일치 — v9(986, 로컬+10.80/실측+4)보다는
  로컬 대비 실측 배율이 낮았는데, 992 베이스 자체가 이미 강해져서 블렌드로
  짜낼 수 있는 분산 여유가 상대적으로 줄었기 때문으로 해석.

## 현재 상태
- **팀 전체 최고(리더보드 실측): 993점** — `es_ws/work/submissions/submit_v11_riskscore_blend.zip`.
  구성: jh_ws v20 CatBoost 6시드(992점 자산) + 신규 LGBM 3시드, 고정 가중치
  (cat 0.78/lgb 0.22, walk-forward 검증) 블렌드 + Platt 보정. XGBoost 미포함.
- (참고, 실측 완료·폐기) `submit_v9_arch_blend.zip` — 986점, 982 레시피
  기반이라 지금은 v11(993)보다 낮은 베이스 위에 있어 더 이상 최선 아님.

## 다음 세션 후보
- 993+블렌드 레시피 위에서 시드 배깅 추가 확장(CatBoost 6→9, LGBM 3→6)
  — §5에서 중단된 것과 같은 시도를 새 베이스 위에서 재시도.
- 진짜 OOF 스태킹은 §4로 확정 기각, 재시도 불필요.
- "무작위 carve-out 가중치 탐색 = 함정" 패턴은 앞으로도 모든 블렌드/스태킹
  실험에 적용 — 반드시 walk-forward(시간 기반) 검증으로만 가중치를 고를 것.
