# 2026-08-15 워크로그

## 배경
- LG Aimers 9 해커톤: 투구 직전 상황/이력 피처로 제구 성공 확률(`control_success`)을 예측하는 과제.
- 내일부터 모델을 여러 번 수정해본 팀원과 협업 예정. 팀 구성을 위해 오늘 최초 제출 1건이 필요했음.

## 한 일

### 1. 데이터 구조 파악
- `open/data_description.md` 및 `open/data/*.csv` 확인.
- 파일 구성: `train.csv`(2019~2024, 1,475,092행×49컬럼), `test.csv`(형식 확인용 5행 샘플, 실제 평가는 2025시즌 비공개 데이터로 서버에서 교체), `sample_submission.csv`, `trackman_history.csv`(2019~2024 투구 실측 로그, 1,793,078행×30컬럼, **미사용**).
- EDA 결과
  - 타깃 분포: `control_success=1` 52.4% / `0` 47.6% (불균형 크지 않음)
  - 시즌별 행 수 균등 (연 24~25만행)
  - 결측치는 대부분 0%대, `asof_*` 이력 피처만 최대 ~2% (콜드스타트 케이스)
  - `pitcher_id` 792명, `batter_id` 830명, 팀 ID 각 13개

### 2. 환경 세팅
- `python3 -m venv`가 sudo 없이 불완전(`python3-venv` 패키지 필요)해서 `pip3 install --user --break-system-packages`로 pandas/scikit-learn/lightgbm/joblib 설치.
- 설치 버전: pandas 3.0.5, scikit-learn 1.9.0, lightgbm 4.7.0, joblib 1.5.3 (주최측 baseline 예시의 `scikit-learn==1.8.0` 등과 버전 다름 — 평가 서버가 `requirements.txt` 기준으로 재설치 가능한 환경인지는 미확인).

### 3. 베이스라인 모델 학습
- `work/train.py`: LightGBM 분류기 학습.
  - 범주형 9개(`top_bottom`, `game_type`, `base_state`, `pitcher_hand`, `batter_hand`, `pitcher_id`, `batter_id`, `pitcher_team_id`, `batter_team_id`)는 category dtype으로 네이티브 처리, 결측치도 LightGBM이 자체 처리.
  - **검증은 랜덤 분할 대신 시간 기반 홀드아웃 사용**: 2019~2023 학습 / 2024 검증. 실제 평가가 2025시즌(미래 시점)이라 랜덤 분할보다 실전 성능을 더 잘 대변한다고 판단.
  - 검증 결과: AUC 0.534 / logloss 0.692 / accuracy@0.5 0.526 — 랜덤(0.5)보다 소폭 나은 수준.
  - feature importance 상위: `batter_id`, `pitcher_id`가 압도적 — 원시 ID 기반 암묵적 평균 학습으로 보이나 개선폭이 크지 않아, 이 태스크 자체의 신호가 원래 약할 가능성.
  - best_iteration 기준으로 전체 데이터(2019~2024) 재학습 후 `work/model/lgbm.pkl` 저장.

### 4. 추론 스크립트 및 제출 패키징
- `work/predict.py`: 로컬 검증용 추론 스크립트. `feature_meta.pkl`에 학습 시 카테고리 목록을 저장해, 추론 시 test에만 있는 미지 범주값은 NaN 처리(`cat.set_categories` 사용, deprecation 경고 회피).
- `work/submit/script.py`: `open/baseline_submit/script.py`와 동일한 인터페이스(`./data`, `./model`, `./output` 경로 규칙)로 작성.
- `open/baseline_submit`(주최측 예시: `script.py` + `model/rf.pkl` + `requirements.txt`)와 zip 루트 구조를 동일하게 맞춰 `work/first_submit.zip` 생성.
- `open/data`를 심볼릭 링크해 end-to-end 실행 검증 완료 (`output/submission.csv`가 `sample_submission.csv`와 동일 포맷으로 정상 생성).

### 5. 제출 및 결과
- `first_submit.zip` 리더보드 제출 → **채점 결과 400점**.
- 팀원 기존 모델은 900점 수준이라고 전달받음. 로컬 검증 AUC(0.534, 거의 랜덤)가 점수에 그대로 반영된 것으로 보임.

### 6. 기타 논의
- 학습 리소스 관련 질문에 답변: 이 태스크(148만행 규모 GBDT 학습) 기준으로는 RAM/CPU가 병목이 아니었음(30GB RAM, 16코어면 충분, 학습 1~2분). GPU는 불필요. 코어 수는 학습 "속도"에만 영향, 결과 점수와는 무관 — 400 vs 900 격차는 리소스가 아니라 피처 엔지니어링/모델링 방식 차이일 가능성이 크다고 판단.

## 다음 액션 (내일 팀원 합류 후)
- 하이퍼파라미터 튜닝 거의 안 한 상태(early stopping만 적용) — 팀원의 기존 900점 모델과 접근 방식 비교 필요.
- `requirements.txt` 버전 불일치(평가 서버 환경 호환성) 확인 필요.
- `asof_pitcher_n`이 작은(콜드스타트) 표본의 success_rate 신뢰도 가중/스무딩 검토.

## 산출물 경로
- 학습/추론 스크립트: `work/train.py`, `work/predict.py`
- 모델: `work/model/lgbm.pkl`, `work/model/feature_meta.pkl`
- 제출 패키지: `work/submit/`, `work/first_submit.zip`

---

## 추가 작업: 모델 개선 (같은 날 이어서)

### 1. `trackman_history.csv` ID 체계 확인 — 선수 단위 join 불가 확인
- `pitcher_id`/`batter_id`/`pitcher_team_id`(train)와 `pitcher_trackman_id`/`batter_trackman_id`/`pitcher_team`(trackman_history)의 값 교집합이 **0개**. ID 범위 자체가 다르고(train은 2만번대 정수, trackman은 5천만~9천만대), 팀도 train은 정수 코드 13개인데 trackman은 `KBO_ARM`/`MIN_*` 등 2군/외국인 포함 26종 문자열 — 완전히 별개의 익명화 체계.
- `data_description.md`의 "1:1로 직접 결합되는 테이블 아님" 경고가 이 얘기였음. 선수별 과거 구속/회전수 피처는 애초에 만들 수 없음.
- 대신 두 데이터에 공통으로 존재하는 **상황 키**(카운트/아웃, 투타 좌우, 이닝/초말)로 그룹핑해 "이 상황에서 투수들이 보통 어떻게 던지는가"라는 컨텍스트 통계 피처로 활용하기로 결정.
- `pitcher_hand`/`batter_hand`(train: 1/2 정수, trackman: Right/Left 문자열)와 `top_bottom`(train: T/B, trackman: Top/Bottom)은 빈도 비율 대조로 매핑 확인(2=Right, 1=Left).

### 2. `work/build_trackman_context.py` 신규 작성
- `trackman_history.csv`를 세 가지 상황 키로 그룹핑해 평균 구속/회전수/무브먼트, 구종군(fastball/breaking/offspeed) 비율, 표본 수를 집계한 룩업 테이블 생성: `count_state`(balls/strikes/outs), `hand_matchup`(pitcher_hand/batter_hand), `inning_state`(inning/top_bottom).
- 결과를 `work/model/trackman_context.pkl`로 저장. 평가 환경에는 `trackman_history.csv` 자체가 없어도(`test.csv`만 교체되는 구조) 이 사전 계산 테이블만 있으면 됨.
- `train.py`/`predict.py`/`submit/script.py`에 `attach_trackman_context()` 추가해 학습·추론 시 동일하게 left join.
- 효과: AUC 0.5344 → **0.5377**로 소폭 상승. 신규 피처 중 `tk_hand_spin_rate_mean`만 top-15 importance 진입 — 방향은 맞지만 개선폭은 작음.

### 3. `pitcher_id`/`batter_id` raw category 제거 — 가장 큰 개선
- gain 기준 feature importance를 확인해보니 `pitcher_id`(30만)/`batter_id`(26만)가 3위 `asof_pitcher_success_rate`(9만)의 3배 이상으로 압도적 — split 횟수뿐 아니라 gain으로도 지배적이었음.
- 그런데 검증 AUC는 랜덤에 가까운 수준 — 792/830개 고카디널리티 범주를 트리가 선수 단위로 잘게 쪼개며 **학습셋에는 과적합, 2024 홀드아웃엔 일반화 실패**하는 패턴으로 판단.
- 실험: 두 컬럼을 완전히 제거하고 재학습 → AUC 0.5377 → **0.5489**로 상승(트랙맨 피처보다 효과 큼). `train.py`에 `DROP_COLS = ["pitcher_id", "batter_id"]` 반영, `CAT_COLS`에서도 제거.
- 최종 feature importance가 `pitcher_team_id`/`batter_team_id`/`season`/`asof_pitcher_n`/`asof_pitcher_reverse_rate` 등으로 건강하게 분산됨(한두 피처 독점 없음) — 과적합 완화의 방증.

### 4. 최종 결과 및 반영
- `metrics.json`: AUC 0.5344 → **0.5489**, logloss 0.6917 → 0.6892, accuracy@0.5 0.5256 → 0.5344.
- `work/model/`, `work/submit/model/`에 `lgbm.pkl`/`feature_meta.pkl`/`trackman_context.pkl` 갱신, `predict.py` 및 `submit/script.py`(임시 `./data` 심볼릭 링크로) end-to-end 로컬 검증 완료.
- `work/submit_v2.zip`(구 `first_submit.zip`) 재생성 완료, 리더보드 제출.
- **리더보드 결과: 403점 → 822점**. 로컬 검증 AUC 개선폭(0.5344→0.5489, +0.0145)과 비교하면 실제 점수 상승폭(+419)이 훨씬 크다 — raw ID 과적합 제거가 로컬 시간분할 검증보다 실제 평가(2025시즌, 완전히 새로운 선수 조합 포함 가능)에서 더 크게 작용한 것으로 보임.

### 5. 다음으로 볼 만한 것
- 팀원 900점대와 격차가 403점 차이에서 **78점 차이**로 크게 좁혀짐 — 남은 격차는 피처/모델링 디테일 수준일 가능성.
- `asof_*` 이력 피처의 콜드스타트(표본 수 적은 케이스) 스무딩은 아직 미착수.
- 하이퍼파라미터 튜닝(현재 early stopping만 적용) 여지 남아있음.

### 6. 실험: 팀 단위 as-of 이력 피처 — 효과 없어 반영 안 함
- `pitcher_team_id`/`batter_team_id`가 top importance에 든 것을 보고, `asof_pitcher_success_rate`와 같은 방식으로 "팀이 이전 시즌까지 누적한 control_success 평균"을 만들어보면 신호가 더 나올 거라 가정하고 시도.
- `work/build_team_history.py` 신규 작성: train.csv에는 정확한 날짜가 없어 투구 단위 as-of는 불가능하지만, 시즌 단위 누적("현재 시즌 미포함, 이전 시즌까지 누적")은 같은 시즌 내 다른 행을 안 봐서 리크 없이 안전하게 계산 가능. `(team_id, season)` 키의 룩업 테이블을 만들어 `team_history.pkl`로 저장(2025용 확장 행 포함).
- `train.py`에 붙여서 재학습 → **AUC 0.5489 → 0.5466로 오히려 하락**. `pt_team_cum_success_rate` 등은 top importance에 들었지만 실질 신호가 아니라 노이즈에 가까웠던 것으로 보임 — 아마 팀별 성공률 차이의 상당 부분이 이미 `season` 피처가 잡고 있는 시즌별(시대별) 추세이고, 그걸 뺀 팀 고유의 잔차 신호는 약한 듯.
- **결론: 반영하지 않음.** `train.py`에서 다시 제거해 AUC 0.5489로 원복 확인. `build_team_history.py` 스크립트는 참고용으로 남겨두되, 학습 파이프라인에는 연결하지 않음.
- 교훈: importance에 새 피처가 들어온다고 곧바로 도움이 되는 건 아니다 — 반드시 홀드아웃 AUC로 전/후 비교해야 함.
- 궁금해서 실제 리더보드로도 확인해봄: 팀 이력 포함 버전을 담은 `score_check_teamhist.zip`을 별도 제출 → **0점**. 반면 팀 이력 제외 버전(822점 버전과 동일 구조)을 `score_check.zip`으로 재제출 → **822점 그대로 재현**, 코드/포맷 문제가 아니라 팀 이력 모델 자체의 문제로 확인.
  - 점검 중 `build_team_history.py`의 실제 버그 하나 발견 및 수정: 2025용 확장 행을 팀별로 "그 팀의 마지막 관측 시즌+1"에 붙였는데, `pitcher_team_id=22`처럼 train.csv에 2019~2020 데이터만 있는 팀은 확장 행이 2021년에 붙어 2025 테스트 시점엔 매칭이 안 되는 문제였음. 모든 팀에 동일하게 `eval_season(=max(season)+1=2025)`로 고정하도록 수정. 다만 이 NaN 자체는 LightGBM이 정상 처리(시뮬레이션으로 확인)하는 걸 확인해서 0점의 직접 원인은 아니었던 것으로 보이고, 근본 원인은 결국 "이 피처가 애초에 도움이 안 됨"으로 정리.
  - **최종 결론: 팀 단위 as-of 이력 피처는 완전히 기각.** 로컬 검증(AUC 하락)과 실제 리더보드(0점) 둘 다에서 일관되게 부정적 → 더 파고들지 않기로 함.
  - 실험용 산출물(`score_check.zip`, `score_check_teamhist.zip`, `model_teamhist/`, `submit_teamhist/`, `work/model/team_history.pkl`) 전부 정리 완료. `submit_v2.zip`(822점 버전)이 현재 최선 상태.

### 7. 실험: 하이퍼파라미터 랜덤서치 — 개선 폭 거의 없음
- `work/tune.py` 신규 작성: train.py와 동일한 피처(trackman context 포함, raw id/team history 제외)와 동일한 시간 기반 검증(2019~2023 학습 / 2024 검증)으로 데이터를 한 번만 로드해두고, `learning_rate`/`num_leaves`/`min_child_samples`/`subsample`/`colsample_bytree`/`reg_lambda`/`reg_alpha`를 무작위로 조합해 25개 트라이얼 + baseline을 early stopping과 함께 비교.
- 결과: 1위 AUC 0.54907(`lr=0.01, min_child_samples=500, colsample_bytree=0.5, reg_alpha=0.5`) vs baseline 0.54887 — **차이 +0.0002**. 상위 5개 트라이얼이 전부 0.5487~0.5491 범위로 사실상 홀드아웃 분할 하나에서 나오는 노이즈 수준이라, 지금 피처셋에서는 하이퍼파라미터가 병목이 아님을 확인.
- **결론: 파라미터 변경 안 함.** 성능 상한을 올리려면 피처(신호) 쪽을 더 파야 함 — 다음은 `asof_*` 콜드스타트 스무딩 등 피처 방향이 우선순위.
- `work/tune.py`는 재사용 가능한 튜닝 도구로 남겨둠. `work/output/tune_results.csv`, `work/tune.log`는 로컬 산출물(gitignore 대상 `work/output/` 및 별도 정리)이라 커밋 대상 아님.

### 8. 실험: `asof_*` 콜드스타트 베이지안 스무딩 — 효과 없어 반영 안 함
- `work/tune_coldstart.py` 신규 작성: `smoothed = (n*rate + k*prior) / (n+k)` 형태로 `asof_pitcher_success_rate` 등 rate/n 쌍 10개에 스무딩 컬럼을 추가로 붙여(원본은 유지) k=5/10/20/50/100/300을 비교.
- 사전 확인: n=0(완전 콜드스타트) 비율은 0.13%뿐이고 중앙값 n은 595 — 애초에 콜드스타트 비중이 작은 데이터였음.
- 결과: k=300(거의 스무딩 안 하는 수준)만 baseline과 동률(0.54891 vs 0.54887, 노이즈 수준)이고 k=5~100은 전부 baseline보다 하락(0.5478~0.5485). 표본이 이미 충분한 대다수 행의 멀쩡한 rate를 전체 평균 쪽으로 당겨버려서 손해가 더 컸음.
- **결론: 반영하지 않음.**

### 9. 실험: trackman 교차 그룹 + 앙상블 — CatBoost가 확실한 개선
- `work/tune_v2.py` 신규 작성, 두 가지 테스트:
  1. trackman 컨텍스트를 카운트×투타좌우 교차 그룹(`tk_cross_*`, 181개 조합)으로 세분화 → **AUC 0.54808, baseline(0.54887)보다 하락.** 반영 안 함.
  2. 앙상블: LightGBM 5시드 배깅은 0.54879로 단일모델(0.54887)보다 오히려 낮았지만, **CatBoost 단일 모델은 0.55005로 +0.0012 확실한 개선.** LightGBM배깅+CatBoost 50/50 블렌드는 0.55019로 근소하게 더 높았지만 CatBoost 단독 대비 차이(+0.00014)는 노이즈 수준.
  - CatBoost가 나은 이유로 추정: `pitcher_team_id`/`batter_team_id`/`game_type` 같은 범주형을 LightGBM의 히스토그램 기반 분할보다 CatBoost의 순서형 타겟 통계(ordered target statistics) 방식이 더 잘 활용하는 것으로 보임.

### 10. CatBoost 모델 채택 — 리더보드 822 → 875점
- `catboost` 신규 설치(`pip3 install --user --break-system-packages catboost`, 1.2.10). 로컬에 원래 없던 패키지라 평가 서버가 `requirements.txt` 기준으로 설치해줄지 리스크가 있었음.
- `work/train_catboost.py` 신규 작성: train.py와 동일 피처(trackman context 3종 포함, raw id/team history/교차그룹 제외)·동일 시간 분할로 학습. iterations=2000, learning_rate=0.03, depth=8, l2_leaf_reg=3.0, early_stopping 100 → best_iteration=348, **검증 AUC 0.55005**.
- `work/submit_catboost/`(script.py + model/catboost.cbm + model/feature_meta.json + model/trackman_context.pkl + requirements.txt) 신규 작성. CatBoost는 범주형을 문자열로 받아서(LightGBM의 category dtype/set_categories와 다름) `build_features`에서 `astype(str)`만 하면 됨. 클린룸(zip 풀고 `./data`에 test.csv만 놓고 실행)으로 로컬 검증 완료.
- `work/submit_v3_catboost.zip`으로 제출 → **리더보드 875점** (LightGBM 버전 822점 대비 **+53점**). 평가 서버가 `requirements.txt`의 `catboost==1.2.10`을 정상 설치해준다는 것도 이번에 확인됨(이전부터 있던 "requirements.txt 버전 호환성 미확인" 우려 일부 해소).
- **현재 최선 버전: `submit_v3_catboost.zip` (875점).** `submit_v2.zip`(LightGBM, 822점)은 참고용으로 유지.
- CatBoost 학습 시 자동 생성되는 `work/catboost_info/`(텐서보드 스타일 학습 로그)는 `.gitignore`에 추가해 커밋 대상에서 제외.

### 11. CatBoost 하이퍼파라미터 튜닝 반영 — 리더보드 875 → 889점
- `work/tune_catboost.py` 신규 작성: CatBoost `learning_rate`/`depth`/`l2_leaf_reg`/`bagging_temperature`/`random_strength`/`border_count`를 무작위로 조합해 15 trial + baseline 비교. 실행 중 세션이 끊겨 baseline + trial 0~6까지만 완료된 상태로 중단됐다가, `random.Random(SEED)` 고정 특성을 이용해 이미 나온 결과는 캐시로 재사용하고 trial 7~14만 이어서 실행해 재개.
- 최고 결과: trial 7 (learning_rate=0.01, depth=8, l2_leaf_reg=20, bagging_temperature=1, random_strength=1, border_count=32) — auc=0.55056, baseline(0.55005) 대비 +0.0005로 로컬 개선폭은 작았음.
- `train_catboost.py`에 해당 파라미터를 `BEST_PARAMS`로 반영해 재학습(검증 auc=0.55056, best_iteration=1053), `model_catboost/catboost.cbm` 및 `submit_catboost/model/catboost.cbm` 갱신.
- `work/submit_v4_catboost.zip` 재생성(클린룸 스모크 테스트 통과) 후 제출 → **리더보드 875 → 889점 (+14)**. 이번에도 로컬 AUC 개선폭(+0.0005)보다 리더보드 상승폭이 상대적으로 크게 나타남(CatBoost 최초 채택 때와 유사한 패턴).
- **현재 최선 버전: `submit_v4_catboost.zip` (889점).** `submit_v3_catboost.zip`(875점)은 참고용으로 유지.

### 12. 실험: raw id 재도입 / 블렌딩 정밀화 / 저카디널리티 교차 피처 — 전부 기각, v4로 원복
- **raw id 재도입** (`work/tune_catboost_rawid.py`): CatBoost의 ordered target statistics가 LightGBM과 달리 고카디널리티 `pitcher_id`/`batter_id`를 과적합 없이 다룰 수 있을지 테스트. 결과 auc=0.54712로 현재 채택 버전(0.55056) 대비 **-0.00344**, best_iteration도 104회에서 조기종료(현재 버전은 1053회) — CatBoost도 이 카디널리티(792/830개)에선 과적합 못 피함. **기각.**
- **블렌드 가중치 정밀 탐색** (`work/tune_blend.py`): 기존 50/50 고정 블렌드(tune_v2.py) 대신 LightGBM 5시드 배깅 + CatBoost 예측을 0.02 간격으로 가중치 스윕. 최적 지점(catboost 74%)에서 auc=0.55070, CatBoost 단독 대비 **+0.00015**로 노이즈 수준. 제출용 패키지에 모델 2개를 유지해야 하는 복잡도 대비 이득이 없어 **기각.**
- **저카디널리티 trackman 교차 피처 3종** (`work/tune_cross2.py`): 5키 풀교차(181조합, 이전 실패)와 달리 `hand_outs`(pitcher_hand×batter_hand×outs_before, 12조합), `count_light`(balls×strikes, 12조합), `hand_inning_half`(pitcher_hand×batter_hand×top_bottom, 8조합) 세 개를 개별 테스트 → 셋 다 baseline보다 근소하게 좋음(+0.00004~+0.00010), 3종 결합 시 auc=0.55083(+0.00027)으로 로컬 검증에서는 가장 유의미해 보이는 결과.
  - `build_trackman_context.py`에 정식 반영하고 재학습(`model_catboost/catboost.cbm` 갱신, 피처 69→93개) → `work/submit_v5_catboost.zip` 제출.
  - **리더보드 결과: 889 → 880점 (-9), 로컬은 올랐는데 리더보드는 내려감.** 지난번 파라미터 튜닝(로컬 +0.0005 → 리더보드 +14, 같은 방향·비슷한 스케일)과 비교하면 방향 자체가 반대로 나온 이례적인 케이스.
  - 원인으로 추정: `tune_cross2.py`가 "2024 검증셋 기준으로 baseline보다 나은 후보만" 골라 조합하는 선택 편향(selection bias) 구조였고, 개별 후보 개선폭(+0.00004~+0.00010)이 애초에 단일 홀드아웃 분할의 노이즈 수준이라 2024 홀드아웃에만 우연히 맞았던 패턴으로 보임. raw id 제거(+0.0112)나 CatBoost 전환(+0.0012)처럼 로컬·리더보드가 같은 방향으로 크게 움직였던 진짜 신호들과는 스케일 자체가 다름.
  - **기각.** `build_trackman_context.py`/`model_catboost/`/`submit_catboost/model/`을 커밋된 v4 상태로 git checkout 원복, `submit_v5_catboost.zip` 및 관련 로그 삭제. 실험 스크립트 3종(`tune_blend.py`, `tune_catboost_rawid.py`, `tune_cross2.py`)은 참고용으로 보존.
  - 교훈: 로컬 AUC 개선폭이 0.0005 이하로 작을 때는 실제 신호가 아니라 단일 검증 분할의 노이즈일 가능성이 높다 — 이 정도 스케일에서는 리더보드 제출 전에 신뢰하지 않는 게 나을 듯.
- **현재 최선 버전: `submit_v4_catboost.zip` (889점).** 변경 없음.
